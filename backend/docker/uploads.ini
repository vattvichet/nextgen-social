; Laravel's StorePostRequest allows up to 5 images x 5MB each. These must
; be at least that generous or PHP silently truncates/rejects uploads
; before Laravel's own (nicer) validation ever runs.
upload_max_filesize = 6M
post_max_size = 30M
max_file_uploads = 10
